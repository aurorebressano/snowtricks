<form method="post" action="{{ path('app_trick_delete', {'id': trick.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?')"; style="margin-top:-5px">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.id) }}">
    <button class="btn"><i class="bi bi-trash" style="color:white" ></i></button>
</form>
